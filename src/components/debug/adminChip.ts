/** Shared look for the local admin overlay bar (breakpoints, token UI, TanStack). */
export const adminChipClassName =
  'flex h-5 items-center rounded border border-white/70 bg-pink-300 text-xs shadow-xl print:hidden'

/** Interactive segment inside the admin bar (no own chip chrome). */
export const adminChipSegmentClassName = 'inline-flex h-full items-center px-1.5'

/** Vertical rule between admin bar segments. */
export const adminChipSeparatorClassName = 'h-3 w-px shrink-0 bg-zinc-950/30'
